# Projeto 🚀 Jalves Espetaria

Um projeto desenvolvido para testar suas habilidades em testes, as APIs criadas possuem alguns pontos de falha, aonde o QA deve realizar os testes de API e localizar os pontos de ajuste.

## Getting Started

`https://github.com/jonathandasilvaalves/jalves_espetaria`

### Backend
1 - Necessário criar os bancos Postgres, podendo utilizar o docker
Ex: docker run --name espetaria -e POSTGRES_PASSWORD=docker -p 5432:5432 -d postgres

2 - Dentro do projeto execute `yarn` ou `npm install` para instalar as dependências

3 - Execute `yarn dev:server` para iniciar o projeto.

## 📝 Licença

Esse projeto está sob a licença MIT. Veja o arquivo [LICENSE](LICENSE.md) para mais detalhes.
